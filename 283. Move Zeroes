class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int p=0,curr=0;
        for (int p = 0, curr = 0; curr < nums.size(); curr++) {
        if (nums[curr] != 0) {
            swap(nums[p++], nums[curr]);
        }
    }
    }
};
